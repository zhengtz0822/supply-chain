# FastAPI 核心
fastapi>=0.110.0
uvicorn[standard]>=0.30.0

# 数据库相关
sqlalchemy>=2.0.0
alembic>=1.12.0

# 数据库驱动 (根据需要选择)
# sqlite (Python 内置，无需安装)
# psycopg2-binary==2.9.9  # PostgreSQL
# pymysql==1.1.0  # MySQL
# cx_Oracle==8.3.0  # Oracle

# Pydantic 配置
pydantic==2.9.2
pydantic-settings==2.6.0

# 其他依赖
python-multipart==0.0.12  # 表单数据支持
python-jose[cryptography]==3.3.0  # JWT 支持
passlib[bcrypt]==1.7.4  # 密码哈希
